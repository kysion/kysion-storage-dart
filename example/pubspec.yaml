name: kysion_storage_example
description: An example project demonstrating the Ksion Storage package.
version: 1.0.0
publish_to: 'none'

environment:
    sdk: '>=2.12.0 <4.0.0'

dependencies:
    flutter:
        sdk: flutter
    kysion_storage:
        path: ../

dev_dependencies:
    flutter_test:
        sdk: flutter
    flutter_lints: ^5.0.0

flutter:
    uses-material-design: true
